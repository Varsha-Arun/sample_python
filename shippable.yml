language: python
  
python: 
  - 2.7

build:
    
  ci:
    - docker --version
    - docker info
    - docker ps 
    
integrations:    
  hub:
      #Docker integration
    - integrationName: docker
      type: dockerRegistryLogin 
